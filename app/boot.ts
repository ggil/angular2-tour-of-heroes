// Access to X Function importing the angular libraries like...
// ..Angular Browser
import {bootstrap}    from 'angular2/platform/browser'
// ..Applications root
import {AppComponent} from './app.component'

// Call bootstrap passing both params
bootstrap(AppComponent);
